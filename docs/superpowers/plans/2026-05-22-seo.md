# Bowser SEO Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make bowser.nz discoverable and shareable through static-first SEO — full meta tags, social cards, structured data, crawl files, and an indexable `/about` URL — then deploy to production.

**Architecture:** All SEO signals live in static files (`index.html`, `robots.txt`, `sitemap.xml`, a committed OG image) plus a tiny per-route meta helper. No prerendering, no framework change. The About page moves from a hash fragment to a real `/about` path so search engines can index it.

**Tech Stack:** Vite + React 18 + TypeScript frontend; Vercel hosting; Playwright MCP for the one-time OG image render; macOS `sips` for image sizing.

**Source spec:** `docs/superpowers/specs/2026-05-22-seo-design.md`

**Branch:** Execute on a dedicated branch. If not already on one: `git checkout -b seo`. The final task merges to `main` to trigger the Vercel production deploy.

**Note on testing:** The bowser frontend has no unit-test framework, and CLAUDE.md says to stick to the documented scope — so this plan does not add one. SEO artifacts (static HTML, config, crawl files) are verified by build success, `view-source` inspection, and browser checks via Playwright MCP, not unit tests. This is the honest verification path for this codebase.

---

## Task 1: Crawl files — robots.txt and sitemap.xml

**Files:**
- Create: `frontend/public/robots.txt`
- Create: `frontend/public/sitemap.xml`

- [ ] **Step 1: Create `frontend/public/robots.txt`**

```
# bowser.nz — https://www.bowser.nz
# All crawlers welcome, including AI assistants (so the project is citeable).

User-agent: *
Allow: /

User-agent: GPTBot
Allow: /

User-agent: ClaudeBot
Allow: /

User-agent: PerplexityBot
Allow: /

User-agent: Google-Extended
Allow: /

Sitemap: https://www.bowser.nz/sitemap.xml
```

- [ ] **Step 2: Create `frontend/public/sitemap.xml`**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://www.bowser.nz/</loc>
    <lastmod>2026-05-22</lastmod>
    <changefreq>daily</changefreq>
    <priority>1.0</priority>
  </url>
  <url>
    <loc>https://www.bowser.nz/about</loc>
    <lastmod>2026-05-22</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.7</priority>
  </url>
</urlset>
```

- [ ] **Step 3: Verify the sitemap is well-formed XML**

Run: `xmllint --noout frontend/public/sitemap.xml && echo "sitemap OK"`
Expected: prints `sitemap OK` with no errors.

- [ ] **Step 4: Commit**

```bash
git add frontend/public/robots.txt frontend/public/sitemap.xml
git commit -m "seo: add robots.txt and sitemap.xml"
```

---

## Task 2: Open Graph share image

**Files:**
- Create: `frontend/og-template.html` (one-time build tool — NOT shipped in the Vite bundle)
- Create: `frontend/public/og-image.png` (1200×630, committed binary)

**Note on fonts:** `og-template.html` loads Fraunces and Inter Tight from Google Fonts. CLAUDE.md's "self-hosted fonts / no Google CDN" rule protects the *shipped site's* load performance; `og-template.html` is never bundled or served to visitors — it is rendered once to a flat PNG with fonts rasterised in. The rule does not apply here.

- [ ] **Step 1: Create `frontend/og-template.html`**

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400&family=Inter+Tight:wght@400;500&display=swap"
      rel="stylesheet"
    />
    <style>
      * { margin: 0; padding: 0; box-sizing: border-box; }
      html, body { width: 1200px; height: 630px; }
      body {
        background: #faf6f1;
        font-family: "Inter Tight", sans-serif;
        color: #3b2f2f;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        padding: 80px;
        position: relative;
        overflow: hidden;
      }
      .frame {
        position: absolute;
        inset: 24px;
        border: 1px solid #e8ddd0;
      }
      .eyebrow {
        font-size: 22px;
        letter-spacing: 0.14em;
        text-transform: uppercase;
        color: #5a4e42;
      }
      .wordmark {
        font-family: "Fraunces", serif;
        font-size: 188px;
        line-height: 0.9;
        letter-spacing: -0.03em;
        margin-top: 6px;
      }
      .tagline {
        font-size: 38px;
        line-height: 1.3;
        color: #4a3e32;
        max-width: 720px;
        margin-top: 22px;
      }
      .footer-row {
        display: flex;
        justify-content: space-between;
        align-items: flex-end;
        font-size: 24px;
        color: #5a4e42;
        position: relative;
        z-index: 1;
      }
      .url { font-weight: 500; color: #8b5e3c; }
      .chart {
        position: absolute;
        right: 0;
        bottom: 0;
        width: 540px;
        height: 300px;
      }
    </style>
  </head>
  <body>
    <div class="frame"></div>
    <svg class="chart" viewBox="0 0 540 300" preserveAspectRatio="none">
      <polyline
        fill="none"
        stroke="#8b5e3c"
        stroke-width="3"
        points="0,220 60,200 120,232 180,150 240,172 300,92 360,124 420,60 480,104 540,44"
      />
      <polyline
        fill="none"
        stroke="#3b2f2f"
        stroke-opacity="0.32"
        stroke-width="2"
        points="0,252 60,242 120,258 180,212 240,228 300,182 360,202 420,162 480,188 540,150"
      />
    </svg>
    <div>
      <p class="eyebrow">New Zealand · global crude</p>
      <h1 class="wordmark">bowser</h1>
      <p class="tagline">The global fuel story, told from New Zealand.</p>
    </div>
    <div class="footer-row">
      <span class="url">bowser.nz</span>
      <span>Brent · WTI · NZ pump prices · MBIE</span>
    </div>
  </body>
</html>
```

- [ ] **Step 2: Render the template to a screenshot with Playwright MCP**

From the repo root, capture the absolute path:

Run: `echo "file://$(pwd)/frontend/og-template.html"`

Then with the Playwright MCP tools:
1. `mcp__plugin_playwright_playwright__browser_navigate` — `url` = the `file://` URL printed above.
2. `mcp__plugin_playwright_playwright__browser_resize` — `width` 1200, `height` 630.
3. Wait ~1s for the Google Fonts to load (call `browser_navigate` again to the same URL if the first paint showed fallback fonts, or use `browser_wait_for` with a short time).
4. `mcp__plugin_playwright_playwright__browser_take_screenshot` — `type` = `png`, `fullPage` = false, `filename` = `og-raw.png`. The Playwright MCP writes screenshots into the repo's `.playwright-mcp/` directory.

- [ ] **Step 3: Normalise the image to exactly 1200×630 and place it**

Run:
```bash
sips -z 630 1200 .playwright-mcp/og-raw.png --out frontend/public/og-image.png
sips -g pixelWidth -g pixelHeight frontend/public/og-image.png
```
Expected: the second command reports `pixelWidth: 1200` and `pixelHeight: 630`.

- [ ] **Step 4: Visually confirm the image**

Open `frontend/public/og-image.png` (Read tool renders it). Confirm: cream background, hairline frame, large serif "bowser" wordmark, tagline readable and not overlapping the chart, coffee-coloured chart lines in the lower right. If the wordmark fell back to a non-serif font, the Google Fonts did not load — re-run Step 2 with a longer wait, then Step 3.

- [ ] **Step 5: Commit**

```bash
git add frontend/og-template.html frontend/public/og-image.png
git commit -m "seo: add Open Graph share image and its template"
```

---

## Task 3: index.html head overhaul

**Files:**
- Modify: `frontend/index.html` (full rewrite — current file is 14 lines)

- [ ] **Step 1: Replace the entire contents of `frontend/index.html`**

```html
<!doctype html>
<html lang="en-NZ">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <title>bowser — New Zealand fuel prices &amp; the global crude oil story</title>
    <meta
      name="description"
      content="Bowser connects global crude oil markets to New Zealand pump prices: Brent and WTI futures, MBIE retail data, and where every fuel dollar goes."
    />
    <link rel="canonical" href="https://www.bowser.nz/" />
    <meta
      name="robots"
      content="index, follow, max-image-preview:large, max-snippet:-1"
    />
    <meta name="author" content="Dhilip Subramanian" />
    <meta name="theme-color" content="#faf6f1" />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Bowser" />
    <meta
      property="og:title"
      content="bowser — the global fuel story, told from New Zealand"
    />
    <meta
      property="og:description"
      content="A public dashboard connecting global crude oil markets to New Zealand pump prices."
    />
    <meta property="og:url" content="https://www.bowser.nz/" />
    <meta property="og:image" content="https://www.bowser.nz/og-image.png" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta
      property="og:image:alt"
      content="Bowser — the global fuel story, told from New Zealand"
    />
    <meta property="og:locale" content="en_NZ" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta
      name="twitter:title"
      content="bowser — the global fuel story, told from New Zealand"
    />
    <meta
      name="twitter:description"
      content="A public dashboard connecting global crude oil markets to New Zealand pump prices."
    />
    <meta name="twitter:image" content="https://www.bowser.nz/og-image.png" />
    <meta name="twitter:creator" content="@sdhilip" />

    <!-- Structured data: site -->
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "WebSite",
        "name": "Bowser",
        "alternateName": "bowser.nz",
        "url": "https://www.bowser.nz/",
        "description": "A public dashboard connecting global crude oil markets to New Zealand pump prices, built on free public data.",
        "inLanguage": "en-NZ",
        "publisher": {
          "@type": "Person",
          "name": "Dhilip Subramanian"
        }
      }
    </script>

    <!-- Structured data: dataset -->
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "Dataset",
        "name": "New Zealand fuel prices and global crude oil — Bowser",
        "description": "Aggregated daily Brent and WTI crude oil futures, New Zealand retail pump prices, the MBIE weekly fuel price waterfall, and fuel stock days-of-cover.",
        "keywords": [
          "New Zealand fuel prices",
          "petrol price",
          "diesel price",
          "Brent crude",
          "WTI crude",
          "MBIE fuel monitoring"
        ],
        "spatialCoverage": "New Zealand",
        "temporalCoverage": "2016/..",
        "isAccessibleForFree": true,
        "url": "https://www.bowser.nz/",
        "creator": {
          "@type": "Person",
          "name": "Dhilip Subramanian",
          "url": "https://www.bowser.nz/about"
        }
      }
    </script>

    <!-- Structured data: author -->
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "Person",
        "name": "Dhilip Subramanian",
        "jobTitle": "Data & AI practitioner",
        "url": "https://www.bowser.nz/about",
        "homeLocation": {
          "@type": "Place",
          "name": "Wellington, New Zealand"
        },
        "knowsAbout": [
          "Data engineering",
          "Data visualisation",
          "D3.js",
          "AI products",
          "Python",
          "React"
        ],
        "sameAs": [
          "https://x.com/sdhilip",
          "https://www.linkedin.com/in/dhilip-subramanian-36021918b/"
        ]
      }
    </script>
  </head>
  <body>
    <div id="root"></div>
    <noscript>
      <div
        style="max-width: 680px; margin: 64px auto; padding: 0 24px; font-family: Georgia, serif; color: #3b2f2f; line-height: 1.6;"
      >
        <h1>bowser — the global fuel story, told from New Zealand</h1>
        <p>
          Bowser is a public dashboard that connects global crude oil markets to
          New Zealand pump prices, built entirely on free public data.
        </p>
        <p>The dashboard has six sections:</p>
        <ul>
          <li>
            <strong>Global crude</strong> — Brent and WTI front-month futures
            over the last twelve months.
          </li>
          <li>
            <strong>NZ retail</strong> — current pump prices for regular 91,
            premium 95 and diesel.
          </li>
          <li>
            <strong>Ten-year history</strong> — a decade of MBIE weekly retail
            prices.
          </li>
          <li>
            <strong>The waterfall</strong> — where each dollar at the pump goes:
            importer cost, ETS, excise and GST.
          </li>
          <li>
            <strong>Fuel supply</strong> — how many days of cover New Zealand
            holds, by fuel type.
          </li>
          <li>
            <strong>News</strong> — New Zealand fuel news alongside global oil
            and geopolitics.
          </li>
        </ul>
        <p>
          Bowser is built and maintained by Dhilip Subramanian, based in
          Wellington, New Zealand.
          <a href="/about">Read more about the project</a>.
        </p>
        <p>
          This dashboard requires JavaScript to display its interactive charts.
        </p>
      </div>
    </noscript>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 2: Verify the head contains every block**

Run:
```bash
grep -c 'application/ld+json' frontend/index.html
grep -c 'og:' frontend/index.html
grep -q '<noscript>' frontend/index.html && echo "noscript OK"
```
Expected: `3` (JSON-LD blocks), `9` or more (og: tags), and `noscript OK`.

- [ ] **Step 3: Commit**

```bash
git add frontend/index.html
git commit -m "seo: full meta, Open Graph, Twitter, JSON-LD and noscript fallback"
```

---

## Task 4: Per-route meta helper

**Files:**
- Create: `frontend/src/lib/head.ts`

This helper owns the `View` type (imported by `App.tsx` in Task 5) and updates the document head when the route changes. It is the single source of truth for per-route title/description/canonical.

- [ ] **Step 1: Create `frontend/src/lib/head.ts`**

```ts
// Per-route document head management. index.html ships the dashboard values
// statically; this helper swaps them when the user navigates to /about and
// back. It is the single source of truth for per-route SEO metadata.

export type View = "dashboard" | "about";

interface RouteMeta {
  title: string;
  description: string;
  canonical: string;
  ogTitle: string;
  ogUrl: string;
}

const ROUTE_META: Record<View, RouteMeta> = {
  dashboard: {
    title: "bowser — New Zealand fuel prices & the global crude oil story",
    description:
      "Bowser connects global crude oil markets to New Zealand pump prices: Brent and WTI futures, MBIE retail data, and where every fuel dollar goes.",
    canonical: "https://www.bowser.nz/",
    ogTitle: "bowser — the global fuel story, told from New Zealand",
    ogUrl: "https://www.bowser.nz/",
  },
  about: {
    title: "About bowser — built by Dhilip Subramanian",
    description:
      "Why I built Bowser: a public dashboard linking global crude oil to New Zealand pump prices. Built in public by Dhilip Subramanian.",
    canonical: "https://www.bowser.nz/about",
    ogTitle: "About bowser — built by Dhilip Subramanian",
    ogUrl: "https://www.bowser.nz/about",
  },
};

function setAttr(selector: string, attr: string, value: string): void {
  const el = document.head.querySelector(selector);
  if (el) el.setAttribute(attr, value);
}

export function setRouteMeta(view: View): void {
  const meta = ROUTE_META[view];
  document.title = meta.title;
  setAttr('meta[name="description"]', "content", meta.description);
  setAttr('link[rel="canonical"]', "href", meta.canonical);
  setAttr('meta[property="og:title"]', "content", meta.ogTitle);
  setAttr('meta[property="og:url"]', "content", meta.ogUrl);
  setAttr('meta[name="twitter:title"]', "content", meta.ogTitle);
}
```

- [ ] **Step 2: Verify it type-checks**

Run: `cd frontend && npm run typecheck`
Expected: completes with no errors. (`head.ts` is not yet imported anywhere; this confirms the file itself is valid TypeScript.)

- [ ] **Step 3: Commit**

```bash
git add frontend/src/lib/head.ts
git commit -m "seo: add per-route document head helper"
```

---

## Task 5: Path routing — /about replaces #/about

**Files:**
- Modify: `frontend/src/App.tsx` (lines 1–50 — imports, routing, view state)
- Modify: `frontend/src/components/Nav.tsx`
- Modify: `vercel.json`

`AboutPage.tsx` is **not** modified: its back buttons call the `onBack` prop, which is `goToDashboard` in `App.tsx`, and that function is updated here to push history state.

- [ ] **Step 1: Update the imports in `frontend/src/App.tsx`**

Replace this block (lines 1–16):

```ts
import { useEffect, useState } from "react";
import { motion } from "framer-motion";

import { loadSnapshot, type Snapshot } from "./lib/snapshot";
import { Hero } from "./components/Hero";
import { CrudeChart } from "./components/CrudeChart";
import { PumpPriceCards } from "./components/PumpPriceCards";
import { WaterfallChart } from "./components/WaterfallChart";
import { RetailTrendChart } from "./components/RetailTrendChart";
import { FuelSupply } from "./components/FuelSupply";
import { NewsColumn } from "./components/NewsColumn";
import { Nav } from "./components/Nav";
import { Footer } from "./components/Footer";
import { LastUpdated } from "./components/LastUpdated";
import { AboutPage } from "./components/AboutPage";
import "./styles/layout.css";
```

with:

```ts
import { useEffect, useState } from "react";
import { motion } from "framer-motion";

import { loadSnapshot, type Snapshot } from "./lib/snapshot";
import { setRouteMeta, type View } from "./lib/head";
import { Hero } from "./components/Hero";
import { CrudeChart } from "./components/CrudeChart";
import { PumpPriceCards } from "./components/PumpPriceCards";
import { WaterfallChart } from "./components/WaterfallChart";
import { RetailTrendChart } from "./components/RetailTrendChart";
import { FuelSupply } from "./components/FuelSupply";
import { NewsColumn } from "./components/NewsColumn";
import { Nav } from "./components/Nav";
import { Footer } from "./components/Footer";
import { LastUpdated } from "./components/LastUpdated";
import { AboutPage } from "./components/AboutPage";
import "./styles/layout.css";
```

- [ ] **Step 2: Replace the `View` type and `readHashView` with path-based routing**

Replace this block (lines 18–22):

```ts
type View = "dashboard" | "about";

function readHashView(): View {
  return window.location.hash === "#/about" ? "about" : "dashboard";
}
```

with:

```ts
function readView(): View {
  return window.location.pathname === "/about" ? "about" : "dashboard";
}
```

- [ ] **Step 3: Update the view state initialiser, listener, and navigation helpers**

Replace this block (lines 24–46):

```ts
export default function App() {
  const [snapshot, setSnapshot] = useState<Snapshot | null>(null);
  const [error, setError] = useState<string | null>(null);
  const [view, setView] = useState<View>(readHashView);

  useEffect(() => {
    loadSnapshot().then(setSnapshot).catch((e) => setError(String(e)));

    const onHashChange = () => {
      setView(readHashView());
      window.scrollTo(0, 0);
    };
    window.addEventListener("hashchange", onHashChange);
    return () => window.removeEventListener("hashchange", onHashChange);
  }, []);

  const goToDashboard = () => {
    if (window.location.hash) {
      history.pushState("", document.title, window.location.pathname);
    }
    setView("dashboard");
    window.scrollTo(0, 0);
  };
```

with:

```ts
export default function App() {
  const [snapshot, setSnapshot] = useState<Snapshot | null>(null);
  const [error, setError] = useState<string | null>(null);
  const [view, setView] = useState<View>(readView);

  useEffect(() => {
    loadSnapshot().then(setSnapshot).catch((e) => setError(String(e)));

    const onPopState = () => {
      setView(readView());
      window.scrollTo(0, 0);
    };
    window.addEventListener("popstate", onPopState);
    return () => window.removeEventListener("popstate", onPopState);
  }, []);

  useEffect(() => {
    setRouteMeta(view);
  }, [view]);

  const goToDashboard = () => {
    history.pushState("", "", "/");
    setView("dashboard");
    window.scrollTo(0, 0);
  };

  const goToAbout = () => {
    history.pushState("", "", "/about");
    setView("about");
    window.scrollTo(0, 0);
  };
```

- [ ] **Step 4: Pass the navigation callback to `Nav`**

In `frontend/src/App.tsx`, find this line (inside the `wordmark-bar` header, ~line 87):

```tsx
          <Nav />
```

Replace it with:

```tsx
          <Nav onAbout={goToAbout} />
```

- [ ] **Step 5: Rewrite `frontend/src/components/Nav.tsx`**

Replace the entire file with:

```tsx
import { useEffect, useState } from "react";

interface Section {
  id: string;
  label: string;
}

interface NavProps {
  onAbout: () => void;
}

const SECTIONS: Section[] = [
  { id: "crude", label: "crude" },
  { id: "pump", label: "nz pump" },
  { id: "history", label: "history" },
  { id: "waterfall", label: "waterfall" },
  { id: "news", label: "news" },
];

export function Nav({ onAbout }: NavProps) {
  const [active, setActive] = useState<string>("crude");

  useEffect(() => {
    // A section counts as "active" when its top crosses the upper third
    // of the viewport. The rootMargin trims the intersection zone so we
    // don't flicker between sections at the seam — one clean hand-off.
    const observer = new IntersectionObserver(
      (entries) => {
        const visible = entries
          .filter((e) => e.isIntersecting)
          .sort((a, b) => a.boundingClientRect.top - b.boundingClientRect.top);
        if (visible.length > 0) {
          setActive(visible[0].target.id);
        }
      },
      { rootMargin: "-30% 0px -60% 0px", threshold: 0 },
    );
    SECTIONS.forEach((s) => {
      const el = document.getElementById(s.id);
      if (el) observer.observe(el);
    });
    return () => observer.disconnect();
  }, []);

  const onSectionClick = (
    e: React.MouseEvent<HTMLAnchorElement>,
    id: string,
  ) => {
    e.preventDefault();
    const el = document.getElementById(id);
    if (el) el.scrollIntoView({ behavior: "smooth", block: "start" });
  };

  const onAboutClick = (e: React.MouseEvent<HTMLAnchorElement>) => {
    e.preventDefault();
    onAbout();
  };

  return (
    <nav className="topnav" aria-label="section navigation">
      <a
        href="/about"
        className="topnav-link topnav-about"
        onClick={onAboutClick}
      >
        about
      </a>
      {SECTIONS.map((s) => (
        <a
          key={s.id}
          href={`#${s.id}`}
          className={`topnav-link ${active === s.id ? "topnav-active" : ""}`}
          onClick={(e) => onSectionClick(e, s.id)}
        >
          {s.label}
        </a>
      ))}
    </nav>
  );
}
```

- [ ] **Step 6: Add the SPA rewrite to `vercel.json`**

Replace the entire contents of `vercel.json` with:

```json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd frontend && npm run build",
  "installCommand": "cd frontend && npm install",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "rewrites": [{ "source": "/about", "destination": "/index.html" }]
}
```

The rewrite is scoped to `/about` only, so static assets (`/og-image.png`, `/robots.txt`, `/sitemap.xml`, `/data/snapshot.json`) are served directly and untouched.

- [ ] **Step 7: Type-check**

Run: `cd frontend && npm run typecheck`
Expected: completes with no errors.

- [ ] **Step 8: Build**

Run: `cd frontend && npm run build`
Expected: build completes; `frontend/dist/index.html`, `frontend/dist/robots.txt`, `frontend/dist/sitemap.xml`, and `frontend/dist/og-image.png` all exist afterwards.

Verify: `ls frontend/dist/robots.txt frontend/dist/sitemap.xml frontend/dist/og-image.png && echo "assets copied"`
Expected: prints `assets copied`.

- [ ] **Step 9: Commit**

```bash
git add frontend/src/App.tsx frontend/src/components/Nav.tsx vercel.json
git commit -m "seo: route About at /about instead of #/about"
```

---

## Task 6: Browser verification

No files change in this task — it verifies the routing and SEO markup behave correctly. Uses the Vite preview server (which does SPA history fallback by default) and the Playwright MCP.

- [ ] **Step 1: Start the preview server**

Run (in the background): `cd frontend && npm run preview`
Note the local URL it prints (typically `http://localhost:4173`). Use that base URL below.

- [ ] **Step 2: Verify the homepage markup is in the served HTML**

Run: `curl -s http://localhost:4173/ | grep -c -e 'application/ld+json' -e 'og:image' -e '<noscript>'`
Expected: a non-zero count covering all three patterns (JSON-LD, og:image, noscript present).

- [ ] **Step 3: Verify crawl files are served**

Run:
```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:4173/robots.txt
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:4173/sitemap.xml
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:4173/og-image.png
```
Expected: `200` for all three.

- [ ] **Step 4: Browser-test routing with Playwright MCP**

Using the Playwright MCP tools against the preview URL:
1. `browser_navigate` to `http://localhost:4173/` — then `browser_console_messages`; expect no error-level messages.
2. `browser_snapshot` — confirm the dashboard hero is present.
3. Click the "about" nav link (`browser_click`) — confirm the URL is now `http://localhost:4173/about` and the About page heading "Who am I?" is visible.
4. `browser_navigate_back` — confirm the URL returns to `/` and the dashboard is shown.
5. `browser_navigate` directly to `http://localhost:4173/about` — confirm the About page renders (this proves the SPA fallback path works).
6. From the About page, click "← back to dashboard" — confirm the URL returns to `/` and the dashboard renders.
7. Back on the dashboard, click a section nav link (e.g. "waterfall") — confirm the page scrolls to that section and the URL stays `/` (section links are scroll anchors, not routes).

Expected: every step behaves as described, with no console errors.

- [ ] **Step 5: Stop the preview server**

Stop the background preview process.

- [ ] **Step 6: No commit**

This task changes no files. If any check failed, fix the relevant earlier task before continuing.

---

## Task 7: Deploy to production

Bowser deploys to `www.bowser.nz` from the `main` branch via Vercel's GitHub integration. Merging this branch to `main` triggers the production deploy.

- [ ] **Step 1: Confirm the working tree is clean and on the feature branch**

Run: `git status --short && git branch --show-current`
Expected: no uncommitted changes; branch is `seo` (or the worktree branch).

- [ ] **Step 2: Merge to main and push**

```bash
git checkout main
git pull --rebase
git merge --no-ff seo -m "seo: discoverability, social cards, structured data, /about URL"
git push origin main
```

- [ ] **Step 3: Wait for the Vercel deploy**

The push triggers a Vercel production build. Wait ~2 minutes, then proceed.

- [ ] **Step 4: Verify the live site**

Run:
```bash
curl -s -o /dev/null -w "robots %{http_code}\n" https://www.bowser.nz/robots.txt
curl -s -o /dev/null -w "sitemap %{http_code}\n" https://www.bowser.nz/sitemap.xml
curl -s -o /dev/null -w "og-image %{http_code}\n" https://www.bowser.nz/og-image.png
curl -s -o /dev/null -w "about %{http_code}\n" https://www.bowser.nz/about
curl -s https://www.bowser.nz/ | grep -c 'application/ld+json'
```
Expected: `200` for robots, sitemap, og-image, and about; `3` JSON-LD blocks on the homepage.

- [ ] **Step 5: Validate structured data and the social card**

- Open `https://search.google.com/test/rich-results`, test `https://www.bowser.nz/` — expect the Person/Dataset/WebSite items detected with no errors.
- Open a social card validator (e.g. `https://www.opengraph.xyz/`), test `https://www.bowser.nz/` — expect the branded OG image and title to render.

- [ ] **Step 6: Output the user's Search Console checklist**

This step produces instructions for the user — it is not code. Present the user with:

1. **Verify ownership in Google Search Console** (`https://search.google.com/search-console`): add `bowser.nz` as a **Domain property**, and add the **DNS TXT record** Google provides to the `bowser.nz` DNS zone. (DNS verification needs no code change and survives redeploys.)
2. **Submit the sitemap**: in Search Console → Sitemaps, submit `https://www.bowser.nz/sitemap.xml`.
3. **Confirm the apex redirect**: in the Vercel project's Domains settings, ensure `bowser.nz` 301-redirects to `www.bowser.nz`, so the canonical host matches the `<link rel="canonical">` tag.
4. **Optional**: run PageSpeed Insights (`https://pagespeed.web.dev/`) on `https://www.bowser.nz/` to record the Lighthouse SEO and performance baseline (CLAUDE.md notes Lighthouse was never measured).

---

## Self-review notes

**Spec coverage check (against `2026-05-22-seo-design.md`):**

- Part 1 (head overhaul) → Task 3 ✓
- Part 2 (OG/Twitter + image) → Task 2 (image) + Task 3 (tags) ✓
- Part 3 (JSON-LD ×3) → Task 3 ✓
- Part 4 (robots.txt + sitemap.xml) → Task 1 ✓
- Part 5 (/about real URL) → Task 5 ✓
- Part 6 (noscript + per-route meta + Search Console) → Task 3 (noscript), Task 4 (helper), Task 5 (wiring), Task 7 Step 6 (Search Console) ✓
- Spec §6 verification items → Tasks 6 and 7 ✓

**Spec correction:** the spec's file table listed `AboutPage.tsx` as modified. It is not — its back buttons call the `onBack` prop, and `goToDashboard` in `App.tsx` (updated in Task 5) handles the history push. No `AboutPage.tsx` change is needed.

**Known limitation (consistent with the static-first approach):** a non-JS crawler that hits `/about` directly sees the homepage's static meta from `index.html`, because `setRouteMeta` runs in JavaScript. Googlebot runs JS and will see the corrected `/about` meta. This is the accepted trade-off of static-first over prerendering, as documented in the spec.

**Type consistency:** `View` is defined and exported once in `head.ts` (Task 4) and imported by `App.tsx` (Task 5). `setRouteMeta(view: View)` matches its single call site `setRouteMeta(view)`. `Nav` takes `{ onAbout: () => void }` (Task 5 Step 5) and is called as `<Nav onAbout={goToAbout} />` (Task 5 Step 4).
